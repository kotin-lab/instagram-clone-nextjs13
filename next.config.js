/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'i.pinimg.com',
      'i.pravatar.cc',
      'images.unsplash.com',
      'plus.unsplash.com',
    ]
  }
}

module.exports = nextConfig
